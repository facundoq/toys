%\documentclass{standalone}
%\usepackage{tikz}
%\usetikzlibrary{matrix,chains,positioning,decorations.pathreplacing,arrows}

%\begin{document}

\tikzstyle{every node}=[font=\large]

\begin{tikzpicture}[
plain/.style={
  draw=none,
  fill=none,
  },
net/.style={
  matrix of nodes,
  nodes={
    draw,
    circle,
    inner sep=10pt
    },
  nodes in empty cells,
  column sep=3cm,
  row sep=40pt
  },
>=latex
]
\matrix[net] (mat)
{
$x$ & $ f^1 $ & |[plain]| \\
|[plain]| & |[plain]| & $g^1$ \\
$y$ &  $ f^2 $ & |[plain]| \\
};

\draw[->] (mat-1-1) --  (mat-1-2);
\draw[->] (mat-3-1) --  (mat-1-2);

\draw[->] (mat-1-1) --  (mat-3-2);
\draw[->] (mat-3-1) --  (mat-3-2);

\draw[->] (mat-1-2) --  (mat-2-3);

%\node[above right] {$f(x,y)=signo(w_x^1 x + w_y^1 y + b)$} 
    
\draw[->] (mat-3-2) --  (mat-2-3);
\draw[->] (mat-2-3) --  +(2,0);

%\node[above right] {$f(x,y)=signo(w_x^2 x + w_y^2 y + b)$};

\end{tikzpicture}


%\end{document}